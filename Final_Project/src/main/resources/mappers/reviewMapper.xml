<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwillbs.mappers.buylistMapper">


	<select id="insertReview">
		insert into review(reviewee, reviewer, rev_score, rev_totalscore, rev_content)
		values(#{sellmemId}, #{buymemId},#{rev_score},#{ rev_totalscore},#{rev_content})
	</select>

<!-- 	<select id="updateScore" resultType="java.lang.Integer"> -->
<!-- 		update review set rev_total = rev_score+rev_totalscore -->
<!-- 		where reviewee = #{reviewee} -->
<!-- 	</select> -->


</mapper>